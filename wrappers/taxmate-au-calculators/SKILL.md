---
name: taxmate-au-calculators
description: Compatibility wrapper for TaxMate AU Calculators. Use for PAYG, BAS, CGT, FBT, super, and stamp-duty source-routing scaffolds.
---

# TaxMate AU Calculators

Use the plugin skill `$taxmate-au:calculators` when available.

Read and follow:

```bash
export TAXMATE_AU_ROOT="${TAXMATE_AU_ROOT:-$(cd "$(dirname "$0")/../.." && pwd)}"
"$TAXMATE_AU_ROOT/skills/calculators/SKILL.md"
```
