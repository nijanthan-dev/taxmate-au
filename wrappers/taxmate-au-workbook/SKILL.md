---
name: taxmate-au-workbook
description: Compatibility wrapper for TaxMate AU Workbook. Use for accountant-facing Excel workbook outputs from reviewed TaxMate AU data.
---

# TaxMate AU Workbook

Use the plugin skill `$taxmate-au:workbook` when available.

Read and follow:

```bash
export TAXMATE_AU_ROOT="${TAXMATE_AU_ROOT:-$(cd "$(dirname "$0")/../.." && pwd)}"
"$TAXMATE_AU_ROOT/skills/workbook/SKILL.md"
```
