---
name: taxmate-au-taxpack
description: Compatibility wrapper for TaxMate AU Taxpack. Use for accountant handoff packs and future PDF/form draft outputs from reviewed TaxMate AU data.
---

# TaxMate AU Taxpack

Use the plugin skill `$taxmate-au:taxpack` when available.

Read and follow:

```bash
export TAXMATE_AU_ROOT="${TAXMATE_AU_ROOT:-$(cd "$(dirname "$0")/../.." && pwd)}"
"$TAXMATE_AU_ROOT/skills/taxpack/SKILL.md"
```
